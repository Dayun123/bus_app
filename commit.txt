Commit msg

Allow user to set acceptable bus distance.

- Add acceptable_bus_distance attribute to location model
- Add acceptable_bus_distance field to form in locations/new.html.erb.
- Whitelist acceptable_bus_distance in Locations controller.
- Change logic in is_nearby? in Locations helper to better deal with
  the fact that the user can now decide the acceptable bus distance.

